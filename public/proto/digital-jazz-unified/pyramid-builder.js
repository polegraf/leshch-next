'use strict';
const compactRoundDiagram=roundDiagram;
roundDiagram=function(){if(tournamentDraft.format!=='Пирамида')return compactRoundDiagram();const count=Number(tournamentDraft.count),rounds=roundNames();return `<section class="pyramid-bracket"><p class="caption">${count} участников · ${rounds.length} раундов · сетка листается вправо</p>${djBracket({aria:'Предпросмотр сетки',columns:rounds.map((name,r)=>({name,tone:'upcoming',matches:Array.from({length:count/2**(r+1)},(_,i)=>({label:r===rounds.length-1?'Финал':'Пара '+(i+1),rows:[{name:r?'Победитель пары '+(i*2+1):'Участник '+(i*2+1)},{name:r?'Победитель пары '+(i*2+2):'Участник '+(i*2+2)}]}))}))})}<p class="caption">Места для принятых участников. Распределение пар: ${tournamentDraft.pairing.toLowerCase()}.</p></section>`;};
